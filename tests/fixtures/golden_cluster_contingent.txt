Marrow Corporation: 2 exposed parts would depend on this supplier only if an unresolved supplier name merge is confirmed (CON-P13, CON-P14). These 2 parts share a supplier only if an unresolved name merge is confirmed; under the confirmed spellings alone they are separate suppliers. Grouping parts by supplier is a modelling judgment rather than a fact, so this is recommended for confirmation and is never applied automatically.
